```c++
#include <bits/stdc++.h>
using namespace std;

const int MX = 1000005;
int dat[MX];
int head = 0, tail = 0;

void push(int x){
	dat[tail++] = x;
}

void pop(){
	head++;
}

int front(){
	return dat[head];
}

int back(){
	if(tail>0)
		return dat[tail-1];
}
void test(){

}
int main(void){
	test();
}
```


### BOJ 10845

```c++
#include <bits/stdc++.h>
using namespace std;
ios_base :: sync_with_stdio(false);
cin.tie(null);
cout.ite(null);

int n, val;
Queue<int> q;
cin >> n;
string cmd;

for (n--){
	cin >> cmd;
	if (cmd == "push"){
		cin >> val;
		q.push(val);
		
	}else if(cmd == "pop"){
		if(q.isEmpty()){
			cout << -1 << '\n';
		}else{
			cout << q.front() << '\n';
			q.pop();
		}
	}else if(cmd == "size"){
		cout << q.size() << '\n';
	}else if(cmd == "empty"){
		cout << q.empty() <<'\n';
	}else if (cmd == "front"){
		cout << q.front() << '\n';
	}else if (cmd == "back) {
		if(q.isEmpty()) {
			cout << -1 << '\n';
		}else {
			cout <<q.back() << '\n';
		}
		
	}
	return 0;
	
}
```