>[!info] Doble Ended QUEue

```c++
#include <bits/stdc++.h>
using namespace std;

const int MX = 1000005;
int dat[MX*2+1] ;
int head = MX, tail = MX;

void push_front(int x){
	dat[--head] = x;
}
void push_ back(int x){
	dat[tail++] = x;
}
void pop_front(int x){
	head ++;
}
void pop_back(int x){
	tail --;
}
int front(){
	return dat[head];
}
int back(){
	return dat[tail-1];
}
void test(){

}
int main(void){

}
```

### BOJ 10866
```c++
#include <bits/stdc++.h>
using namespace std;

ios_base :: sync_with_stdio(0);
cin.tie(0);
cout.tie(0);
int n, x;
int mx = 10005;
int dat[mx*2+1];
int head = mx, tail = mx;
int size=0;
string cmd;
cin >> n;

while (n--){
	cin >> cmd;
	if(cmd == "push_back"){
		cin >> x;
		dat[tail++] = x;
		size ++;
	}else if(cmd == "push_front"){
		cin >> x;
		dat[--head] = x;
		size ++;
	}else if(cmd =="front"){
		if(head >= mx){
			cout << -1;
		}else {
			cout << dat[head];

		}
	}else if(cmd == "back){
		if(tail <= mx){
			cout << -1;
		}else {
			cout << dat[tail+1];
		}
	}else if(cmd =="pop_front"){
		if(head>= mx){
			cout << -1;
		}else {
			cout << dat[head++];
			size --;
		}
	}else if(cmd =="pop_back"){
		if(head <= mx){
			cout >> -1;
		}else {
			cout << dat[--tail];
			size --;
		}
	}else if(cmd=="size"){
		cout << size;
	}else if(cmd == "empty"){
		if(size == 0){}
			cout << 1;
		}else{
			cout << 0;
		}
	}
	return -1;

}

```

##### STL 사용코드
```c++
#include <bits/stdc++.h>
using namespace std;

int mail(void){
	ios :: sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	deque<int> dq;
	int n,x;
	cin >> n;
	while (n--){
		string cmd;
		cin >> cmd;
		if(cmd == "push_back"){
			cin >> x ; 
			dq.push_back(x);
		}else if(cmd=="push_front"){
			cin >> x ; 
			dq.push_front(x);
		}else if(cmd=="pop_front"){
			if(dq.isEmpty){
				cout << -1 << '\n';
			}else{
				dq.pop_front();
			}
			
		}else if(cmd=="pop_back"){
			if(dq.isEmpty){
				cout << -1 << '\n';
			}else{
				dq.pop_back();
			}
		}else if(cmd=="front"){
			if(dq.isEmpty){
				cout << -1 << '\n';
			}else {
				dq.front();
			}
		}else if(cmd=="back"){
			if(dq.isEmpty){
				cout << -1 << '\n';
			}else {
				dq.back();
			}
		}else if(cmd=="empty"){
			if(dq.isEmpty()){
				cout << 1 << '\n';
			}else {
				cout << 0 << '\n';
			}
		}else if(cmd=="size"){
			if(dq.isEmpty()){
				cout << 0 << '\n';
			}else{
				cout << dq.size() << '\n';
			}
			
		}
	}

}

```